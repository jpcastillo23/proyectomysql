CREATE DATABASE ducati;

CREATE TABLE Persons
(
PersonID int,
LastName varchar(255),
FirstName varchar(255),
Address varchar(255),
City varchar(255)
); 

--SELECT DISTINCT ESTUDIANTE.ID, NOMBRE, PROMEDIO FROM ESTUDIANTE, UNIVERSIDAD WHERE ESTUDIANTE.ID = UNIVERSIDAD.ESTUDIANTE_ID AND ESTUDIANTE.PROMEDIO > 90;
-- CREATE DATABASE ducati;
-- SELECT DISTINCT ESTUDIANTE.ID, NOMBRE, PROMEDIO FROM ESTUDIANTE, UNIVERSIDAD WHERE ESTUDIANTE.ID = UNIVERSIDAD.ESTUDIANTE_ID AND ESTUDIANTE.PROMEDIO > 90;
